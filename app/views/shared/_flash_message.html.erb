<% flash.each do |message_type, message| %>
  <div class="alert-<%= message_type %> fixed flex items-center w-full h-8 pl-4 text-white bg-black/50 animate-flash z-20">
    <%= message %>
  </div>
<% end %>
